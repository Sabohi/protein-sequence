import styled from 'styled-components';
import ClipLoader from 'react-spinners/ClipLoader';

export const StyledContainer = styled.div`
  position: relative;
`;

export const LoaderComponent = styled(ClipLoader)`
  color: #32cd32;
`;
